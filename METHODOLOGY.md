# 判定口径与算法说明

这份文档用**大白话**写清楚「台风与蝉」怎么判断你受不受影响的全部口径和阈值，方便气象、应急、水文等专业人士**不看代码也能复核、挑错、一起维护**。判定不追求花哨，只追求**站得住、可追溯、贴合真实体感**。

发现不合理处，欢迎在 [Issues](https://github.com/gaosheng0201-glitch/typhoonandcicada/issues) 提出。

> 所有阈值与判定都在 **`docs/assess-core.js`**（前端与校准脚本共用的纯函数核心，2026-08-28 从 panel.js 抽出；estGaleRadius 权威表在 data.js），本文与代码一一对应；改代码请同步改本文与 CHANGELOG。回归自检：`node scripts/sanity_check.mjs <tfid> [--at ...] [--event] [--sweep]`。

---

## 一、核心理念

**台风等级 ≠ 你受影响的程度。距离和路径才是关键。**

一个「超强台风」在几百公里外掠过，可能对你毫无影响；一个减弱的热带风暴正面压过，反而可能内涝。所有判断都以**你与台风路径的真实几何关系 + 本地逐时风雨预报**为准，不拿台风最高等级吓人。

## 二、最近点与路径

取**整条轨迹（历史实况 + 预报）上离你最近的点**作为「最近点」，它锚定距离、风雨窗口和阶段。

> ⚠️ 已修正的坑：早期只看「最近 4 个实况点 + 预报」，会把**几小时前已从你身边掠过、台风此刻已在数百公里外**的城市误判成「路径不经过」——连温州（登陆点）都可能显示「不经过」。现改为全轨迹找最近点：若历史最近点更近且已成过去，说明台风已从你身边过去，用它作真实最近点。

## 三、阶段：来之前 / 影响进行中 / 已减弱 / 已过境

用**本地逐时数值模式序列**判定风雨开始/结束（外围雨带常远早于风圈几何，巴威在温州提前约 8 小时下雨）：

- **风雨窗口**：逐小时里 ≥1.5 mm/h 降雨 **或** ≥8 级阵风（62 km/h）算「台风风雨」；间断超过 6 小时即分段，取含最近时刻的那段。
- **「已过境」的判据**：不仅本地风雨窗口已结束，**还要求台风最近点确已成为过去**——否则模式雨窗中途的一段空档，会把「台风其实还在你最近点」误判成过境。
  - 例：某城台风此刻仍在约 70 km 处、最近点刚在数小时前掠过、路径继续北上远离 → 判「已过境，但雨还没停」，是有支撑的（最近点确在过去）。
- **已减弱**：最近点已过 + 此刻明显弱于本次峰值 → 标「已过峰值，正在减弱」。

**已发生 / 未来 分离（预警的主角是「接下来」）**：窗口内的雨量拆成「已下」与「还会下」两个量。**预警要回答的是接下来还有多少**，而已经下过的雨是既成事实、不该被改写——这正是「同一场台风昨天说 186 mm、今天说 0 mm」那种自相矛盾的根源。界面主推「接下来还有约 N mm」，已下量作背景。稳定性实测（杭州/上海/宁波跨 3 天 7 个时刻）：过程总量恒定、已下单调增、待下单调减、档位不跳变、相关性从不翻转。

**事件时间线层与「解除态」（2026-08-28）**：assess 是「时刻快照」，而一场台风对一座城市是**完整事件**。`assessEvent`（assess-core.js）用**同一个 assess** 沿事件跨度每 3h 自采样——每个采样点以「当时所知」取景（`stormAsOf`：轨迹截断到该刻、之后实况充当完美预报，否则历史强点会丢失）——产出事件对象：档位曲线、**事件峰值**（不随时刻回落）、12 小时前的档位。

据此，危险回落**明确宣布而非静默降档**（官方预警本就是「发布→解除」的范式）：峰值已成过去、当前档明显低于峰值时，显示「**『高危』已解除 —— 本次风险最高曾达『高危』（N 小时前），最强时段已过，当前按『准备』行动**」。不拖慢回落（那是对用户说谎），也不抹掉事件事实（峰值留在话里）。

守卫：本地模式数据未就绪时**不做事件断言、不缓存**——快照可渐进显示（数据到位即重渲覆盖），但拿「演示估算」跑事件采样会得出「曾达高危」这类错误的事件级结论（实测丽水全程①却因此误挂解除条）。宁可暂时不显示，等真实数据到位。

## 四、本地实际影响分档（①外围掠过 / ②明显影响 / ③正面重创）

全站最核心、最需要专业复核的一环。判定**锚在国家标准**，并按土壤湿度动态调整。风、雨任意一臂够线即升档。

**风臂——用本地实际预报的峰值阵风，对齐《台风预警信号》风级：**

> ⚠️ 已修正的系统性过度预警：风臂原先拿「中心距离 <200km + 中心强度 ≥10 级」当代理。
> 巴威回测（2026-07-12，浙江 8 市）暴露了它——巴威是 10~13 级、各市都在 200 km 内，
> 于是 **8 城全被判「戒备」**，可实测峰值阵风只有 8~9 级、嘉兴过程雨量仅 23.5 mm。
> **中心多强 ≠ 你这里多大风。** 改用本地峰值阵风后：温州（10 级）、台州（12 级）
> 仍为「戒备」，丽水/金华降为「准备」，杭州/宁波/绍兴/嘉兴降为「关注」。
> 仅在无模式数据时才回退到几何代理；另保留安全网——强台风中心 <100 km 正面压境时
> 直接进「高危」，防模式低估极端阵风。

| 峰值阵风 | 对应官方信号 | 判档 |
|---|---|---|
| ≥ 阵风 12 级（≥118 km/h） | 橙色·严重 | ③ 正面重创 |
| ≥ 阵风 10 级（≥89 km/h） | 黄色·较重 | ② 明显影响 |
| < 阵风 10 级 | 蓝色·一般及以下 | ① 外围/轻微 |

**雨臂——对齐《降水量等级》GB/T 28592-2012 / 暴雨预警，且随土壤湿度动态下调：**

累计雨量取「过程雨量」与「相关的未来 24h 残余雨」中较大者。

| 累计雨量 | 干土基准 | 判档 |
|---|---|---|
| ≥ 大暴雨基准 × 湿度系数 | 100 mm | ③ 正面重创 |
| ≥ 暴雨基准 × 湿度系数 | 50 mm | ② 明显影响 |
| 更少 | — | ① 外围/轻微 |

**两套分档的分工**：面板顶部的**风险档**（关注 / 准备 / 戒备 / 高危）回答「你现在该做到哪一步」，雨臂基准 60 / 150 / 250 mm；上面的**影响分档**（①②③）回答「实际影响有多重」，基准 50 / 100 mm，用于过境后的善后措辞。两者的雨臂**都按土壤湿度动态下调**（同一口径，避免出现「土壤饱和了却因为总量微调跨回阈值就降档」）。唯一例外：**「高危」的雨量线不下调**——它的文案涉及听从转移安排，不该因土壤湿度把整片区域推到转移级别（实测：土壤饱和时 187 mm 就会触发，过度了）。

**表达上不做「总量 − 已下 = 还剩」的减法**：模式雨量本身误差就大、机构还会不断调整，减出来的「还剩 20 mm」是假精度。改为给**进度感**——刚开始 / 正处在较强时段 / 已过大半接近尾声 / 已基本结束，这个定性判断稳健得多，也正是用户真正需要的「影响到哪一步了」。

**本地气候标定（补上「脆弱性」这一维）**：绝对阈值是全国一把尺，但**同样的雨在不同城市根本不是一回事**——下 100 mm，在屏东县排第 **16** 百分位（家常便饭，中位数 216 mm），在北京却是**有记录以来最大**（中位数仅 1.9 mm）。一座城市自己的台风降雨史，天然编码了它的气候背景与承受力（灾害学里的 Vulnerability）。故在「历史对照」中给出：

> 本次 162 mm 在本地台风降雨史中排 **第 95 百分位**（近 21 场台风样本 · 在本地属罕见量级）

数据来自 `rain-percentile.json`（由 `build_rain_percentile.py` 从 262 城 × 85 场台风的 ERA5 过程雨量聚合，177 城收录、140 城样本 ≥10 场）。**目前只用于表达与标定，不直接改判档**——分位数是「罕见度」，不等于「后果严重度」；ERA5 对极端峰值偏平滑，样本量也有限。样本 <10 场自动不显示，退回绝对阈值口径。

## 五、动态临界雨量：同样的雨，土越湿越危险

这是官方**山洪 / 地质灾害气象预警**的通行做法（「前期影响雨量 + 动态临界雨量」）：土壤越接近饱和，同样的雨越易致灾，**致灾门槛越低**。

- **前期影响雨量 Pa**（土壤湿度代理量）：`Pa = Σ Kⁱ · P(i天前)`，退水系数 `K = 0.85`，越近的雨权重越高（模拟土壤慢慢晒干）。**不用「过去 14 天简单累加」**——那会把一周前的雨按原价算，高估湿度。
- **土壤饱和度**：`w = min(1, Pa / Wm)`，土壤最大蓄水量 `Wm = 100 mm`（湿润区经验值）。
- **动态门槛**：`实际门槛 = 基准 × (1 − 0.4 × w)`。干土（w=0）用满基准（暴雨 50 / 大暴雨 100）；土壤饱和（w=1）门槛最多下调 40%（→ 30 / 60 mm）。

> 例：某城过程雨仅 1 mm、未来 24h 残余 15 mm、土壤 w≈0.44 → 明显影响线 = 50×(1−0.4×0.44) ≈ 41 mm，15 mm 远够不着 → 判 ① 外围/轻微。这与「过程雨 1mm 却报明显影响」的旧结果相反，也更贴合体感。

## 六、降雨相关半径：风的范围 ≠ 雨的范围

判断「本地这场雨算不算台风的账」，**不能用 7 级风圈**——风圈衡量的是风，而台风的螺旋雨带向外延伸数百公里（台风水平尺度 500~1000 km），**中心还在数百公里外时，外围暴雨可能先到**。

> ⚠️ 已修正的重大漏报：白海豚（8 级、深入内陆）中心距杭州 257 km，超出「风圈 160×1.25 = 200 km」而被判「与台风无关」，页面显示「预报路径不经过你所在区域」——可杭州当时正连下三天、累计 186 mm 大暴雨，官方同期把江浙沪划入台风「危险半圆」。同样被漏掉的还有宁波（199 mm）、上海（193 mm）、南京（102 mm）。

- **降雨相关半径** = 7 级风圈 × 2.5，并夹在 **[300, 600] km**。下限保住弱残涡的强降雨（美莎克在广西停滞下 600 mm+ 的教训），上限防止「全国的雨都算台风的账」。
- 满足该半径（或本地已在风圈内）才建立风雨窗口、计入累计雨量与「未来 24h 残余降雨」的提醒——避免台风过境后普通的梅雨 / 对流雨被误顶档位。
- **风的判定仍用风圈**：影响分档的风臂、`inRange`、停留型判据都不变。两条轴分开，各用各的尺度。

9 城实测（白海豚）：杭州 257 / 上海 348 / 南京 348 / 宁波 197 / 合肥 233 km 判「相关」，实际降雨 50~200 mm；广州 771 km 判「不相关」，实际仅 1.2 mm——补上漏报又没有矫枉过正。

**象限订正：降水不是一个圆。** 白海豚实测——相对移动方向的**右前象限** 5 城平均 **147 mm**，左侧 3 城平均仅 **11 mm**（13 倍），而距离完全解释不了这个差异（福州 159 km 只 11 mm，上海 348 km 却 193 mm）。文献亦证：强降水位于路径前进方向右侧，对流集中于移动方向右前象限（低空急流与水汽输送在右侧更强）。故相关半径乘一个方向因子：

```
方向因子 = 0.65 + 0.35 × cos(相对移动方向的方位角 − 45°)     // 右前最大、左后最小，下限 150 km
```

效果：福州（左后）阈值压到 150 km、武汉（左前）249 km → 均正确排除；杭州 / 上海 / 宁波 / 合肥（右前）全部保留——**同时少漏报、少误报**。

## 七、海浪（沿海 / 海岛）

**几百公里外的台风，涌浪照样致命。** 本地风雨看着「没事」，外海涌浪却能扑上礁石、堤坝——这就是「疯狗浪」在看似平静时卷走海钓、赶海者的机制。海况是**独立于风雨的一条危险轴**，即使风雨判「不经过」也要报。

- **数据**：[Open-Meteo Marine API](https://open-meteo.com/en/docs/marine-weather-api)（有效波高、涌浪、周期），海格点才有数据，内陆自动为空。
- **外海暴露采样点**：县城中心常落在岛群 / 港湾内侧遮蔽格点（嵊泗县城仅约 2.5m，外海却 7.5m）。所以**不查县城坐标，查预存的外海暴露点**——由 `fetcher/build_coastal.py` 对每个沿海区县向外海撒一圈候选点、挑最开阔的那个存入 `docs/data/coastal.json`。
- **取值**：采样点 `[此刻−6h, 此刻+48h]` 内的峰值有效波高。
- **分级**（浪级 GB/T + 官方海浪预警对照）：

| 峰值有效波高 | 浪级 | 提示强度 |
|---|---|---|
| < 2.5 m | — | 不提示 |
| 2.5–4 m | 大浪 | 涌浪扑岸，别靠近礁石 / 消浪堤，海钓、赶海暂停 |
| 4–6 m | 巨浪 | 渔船回港、养殖排上岸，远离海边 |
| 6–9 m | 狂浪（近海达海浪黄色级） | 严禁出海，别去海边看浪 / 拍浪 |
| ≥ 9 m | 狂涛（近海橙色级以上） | 极危，低矮海岸需撤离 |

> 官方《海浪预警》有效波高分级（自然资源部）——近岸：蓝 2.5–3.5 / 黄 3.5–4.5 / 橙 4.5–6.0 / 红 ≥6.0 m；近海：黄 6–9 / 橙 9–14 / 红 ≥14 m。本站采样点在外海（≈近海海域），故按近海口径对照，文案面向近岸活动者。

## 八、雨下得久：停留型台风 / 风雨持续时间长（两条轴，对用户合并成一句）

台风最致命的常常不是风，而是**在你头上待太久**——雨量随滞留时间累积（本项目的缘起，2026 年美莎克停滞广西、累计 600 mm+，正是这一类）。

内部分**两条不同的轴**（别混为一谈），但**对用户只呈现一条**——用户要的是「这个阶段对我什么影响」，
不是「台风移速多少」（移速是路径图上点位需要的技术参数）：

- **A. 停留型**（台风本身赖着不走）：判据见下方三条腿。
- **B. 风雨持续时间长**（你这里的雨要下很久，与台风快慢无关）：本地风雨窗口 ≥24 h 且雨量达暴雨量级。
  白海豚移速 17.5~36 km/h 一点不慢，却让上海连下 52 小时、208 mm——只看移速会 100% 漏判。

界面统一显示：「**风雨持续时间长 —— 预计持续两天以上，雨量会不断累积，重点防内涝而不是防风**」，
仅在移速真慢时补一句灰色的「（台风移动缓慢，约 N km/h）」作为原因说明。

**A（停留型）的判据**要成因 + 后果都够，三条腿缺一不可：

- **① 距离闸门**：中心必须确实进入你的影响半径。远在洋面、移速慢的新生台风不贴此标签（否则刚生成的弱台风会误报）。
- **② 成因——移速真慢 或 滞留够久**（二选一即可）：
  - **移速门槛 10 km/h**：西北太平洋台风常态移速 15～20 km/h（快的可达 30），真正的「移动缓慢」是 5～10 km/h（海葵登陆后 5–10、烟花平均 < 10）。早期版本取 18 km/h，正落在常态区间中段，几乎所有台风都会被误判为停留型——已收紧到 10。
  - **滞留时长 ≥ 24 h**：移速慢只是造成长时间影响的手段之一。移速不慢、但路径贴着你走的台风同样是长时间碾压，这类原先会漏判，现由**本地风雨窗口时长**直接判定。
    > **实证（白海豚 2026-08，上海内涝）**：同为影响上海，巴威中心更近（218 km）、阵风更大（93.6 km/h），过程雨量却只有 23.5 mm；白海豚中心更远（348 km）、阵风更小（78.8 km/h），过程雨量 208.7 mm——**差 8.9 倍**。拆开看：巴威实质降雨（≥1.5 mm/h）只持续 **4 小时**，白海豚持续 **46 小时**；而两者的**平均雨强反而是白海豚更低**（4.5 vs 5.9 mm/h）。**差别几乎全在「下了多久」**——这正是内涝的机制：排水系统扛得住短时强降雨，扛不住连续两天不停。而白海豚移速 17.5～36 km/h **一点都不慢**，只看移速会 100% 漏判；靠「本地风雨滞留 52 小时」这条轴才报出「风雨持续时间长」。
- **③ 后果——过程雨量达到暴雨量级**（50 mm 基准，同样随土壤湿度下调）：**停留型真正致灾的是「雨长时间累积在同一处」**，滞留 30 小时却只下 20 mm 并不构成威胁。只讲滞留不讲累积，等于把「慢」本身当成危险。

> 自测：美莎克式（8 km/h、600 mm）✓；滞留 30 h 但仅 20 mm ✗（雨量不够）；移速 20 km/h 却贴着走 28 h、200 mm ✓；荔湾（16 km/h、14 h）✗；远洋慢台风 ✗（中心不够近）。同样下 30 mm，干土时 ✗、土壤饱和时 ✓——与动态临界雨量自动联动。

命中后会显示「停留型台风」提示，并在影响进行中追加「被困数天怎么撑」的清单（项目缘起的美莎克场景）。

另有一处**预防性**例外：**移速真慢（<10 km/h）且中心已进影响半径**时，风险等级直接至少抬到「戒备」、无模式数据时的几何回退雨量也上浮——这一项**只看移速、不要求雨量与滞留时长**。理由：一个真正缓慢压境的台风本身就是高危信号，此时模式雨量往往还没跟上，宁可提前警戒。

## 九、当前参数一览（可调旋钮）

| 参数 | 含义 | 当前值 |
|---|---|---|
| `SOIL_K` | 前期影响雨量退水系数 K | 0.85 |
| `WM_SOIL_MM` | 土壤最大蓄水量 Wm | 100 mm |
| `SOIL_DROP` | 湿土最多下调门槛比例 | 0.4（40%） |
| 明显影响·雨基准 | 暴雨线 | 50 mm |
| 正面重创·雨基准 | 大暴雨线 | 100 mm |
| 风险档·风臂（用本地实际峰值阵风） | ②/③/④ 门槛 | 阵风 89 / 118 / 150 km/h（10/12/14 级） |
| 风险档·风臂安全网 | 强台风正面压境直接④ | 中心 <100 km 且 ≥14 级 |
| 影响分档·风线 | ②明显影响 / ③正面重创 | 阵风 10 级(89) / 12 级(118) |
| 长雨提示门槛 | 本地风雨窗口时长 + 雨量 | ≥24 h 且 ≥50 mm×(1−0.4w) |
| 降雨相关半径 | 风圈倍数（夹在 300~600km） | 2.5× |
| 风雨窗口·雨阈 / 风阈 | 计入台风风雨的最低雨强 / 阵风 | 1.5 mm/h / 62 km/h(8级) |
| 窗口分段间断 | 超过则分段 | 6 h |
| METAR 实况采用 | 距离 / 时效上限 | 55 km / 150 min |
| 海浪·提示下限 | 峰值有效波高 | 2.5 m |
| 海浪·分级 | 大浪 / 巨浪 / 狂浪 / 狂涛 | 2.5 / 4 / 6 / 9 m |
| 海浪·采样窗口 | 外海采样点峰值波高时段 | 此刻−6h ～ +48h |
| `SLOW_KMH` | 停留型·移速门槛 | 10 km/h |
| `STALL_HOURS` | 停留型·滞留时长门槛 | 24 h |

7 级风圈估算半径（官方停发半径时按当前风力估，km）：≥16级 400 / 14级 350 / 12级 300 / 10级 230 / 8级 160 / 6级 110 / 更弱 70。

## 十、已知局限（欢迎指正）

- **模式雨量 / 阵风存在不确定性**，尤其**沿海、海岛测点的模式阵风可能偏高**（例：离台风数百公里的海岛仍报 9–12 级阵风，或为暴露地形 + 模式偏差）。此时纯风驱动的档位可能偏高，属上游数据特性，非判定逻辑问题。
- **海浪采样点是预存的固定外海点**，反映该岸段暴露海域的浪，非你脚下沙滩的破碎波高；`coastal.json` 由建表时的开阔度启发式挑点，个别复杂岸线可能选偏。
- **前期影响雨量 Pa 用日雨量近似**，未含蒸散发、土壤类型、地形差异；`Wm=100` 是湿润区经验值，未按区域细化。
- **不含径流 / 汇流水文模型**，动态临界雨量是官方方法的简化版，用于「档位提示」，非定量山洪预报。
- 一切**以气象与应急部门正式发布为准**，本站为公益参考，不发布预警。

## 十一、impact.json：多期 AI 预报的一致性统计（实验）

把 FNV3（Google DeepMind）每 12 小时一期的 ensemble_mean 预报当作**滞后集合**
（lagged ensemble）：同一个未来时刻，先后几期预报各说了什么——期期一致，结论可以
说硬一点；期与期吵架，就明说「还看不准」。风暴刚形成时预报期数少、时效远，不确定
本身就是要交付的信息，不遮掩。

- **命中概率**：最近 ≤6 期预报里，几期把中心送进城市影响半径 → `hits/of` 与
  high / medium / low 档。影响半径 = 150 km（大风圈典型量级）+ 该时效的
  **历史实测误差**（FNV3 对官方实况的平均偏差，按时效插值）——误差随时效放大，
  半径与嘴上的把握度同步放松。
- **诚实性封顶**：影响时刻在 5 天外封顶 low、3 天外封顶 medium；期数不足 4 期
  （刚生成的风暴）说不出 high。回测巴威（202609）实证：07-03 各期一致指向舟山，
  实际路径其后北偏数百公里——一致 ≠ 准，长时效必须压住把握度。
- **收敛度**：取 now+24/48h 两个未来时刻，比较最近 3 期与更早 3 期预报位置的
  散布（km）：≤120 已收敛 / ≤300 趋于一致 / 更大则明说分歧仍大；散布环比放大时
  提示「最近几期分歧反而加大」。
- **回放模式**：`build_impact.py --at 2026-07-11T12:00Z` 可把「现在」拨回任一
  历史时刻复盘当时会说什么（防穿越：只用 init 不晚于该时刻的期），输出到
  `impact.replay.json`，不覆盖正式文件。

研究性数据（CC BY 4.0，关联时刻 48 小时后），仅供参考，避险决策以官方预警为准。

## 十二、consensus.json：多源路径融合与校准的不确定性锥（实验）

自研算法层（`build_consensus.py`）：把 5 家官方机构（中国/日本/美国/中国香港/
中国台湾）与 FNV3 AI 多期预报融合成一条「共识路径」，并给出**用历史误差校准的**
不确定性半径。原料是 archive/ 原始快照——每个历史轨迹点都挂着**当时**各机构
发布的预报，一场台风自带完整检验集。

- **检验**：每个发布时刻 × 每源 × 每预报点对照后来实况 → 每源每时效桶的平均
  误差表；另按发布距今指数衰减（τ=48h）得「这场台风谁报得准」的近况技巧。
- **融合**：各源最后一次发布插值到逐 6h 网格，权重 ∝ 1/(该源该时效历史误差²+80²)，
  加权平均成共识位置，同时输出源间散布 spread。
- **校准锥**：把融合器在每个历史发布时刻回放 → 融合误差分布的 68/90 分位数为
  锥半径，再按「当前散布/历史散布」缩放（clamp 0.6–1.8）——机构一致时锥收窄、
  分歧时放宽，宽窄是算出来的，不是画出来的。
- **验收**（`--backtest <tfid>`，定标/验收分离：前 60% 发布时刻定标、后 40% 验收）：
  巴威（202609）实测——r68 覆盖 84%、r90 覆盖 95%（偏保守方向的诚实）；融合误差
  在各时效上接近**事后才知道的**最优单源（事前无人知道哪家会赢），+54h/+60h 处
  融合（45/52 km）明显优于任何单源（≥97 km）。
- 检验集由 fetch.py 瘦身为 docs/data/verify/{tfid}.json（只留时刻/经纬三元组，
  随数据一起入库），云端 Actions 与本地 update.sh 都能跑；archive/ 原始快照
  仅作本地后备。前端消费时同样遵守新鲜度保护。

非官方预报综合，仅供参考，以气象部门发布为准。

## 十三、官方参考标准

- 《台风预警信号》分级（中国气象局）：蓝 / 黄 / 橙 / 红，按 24 / 12 / 6 h 内平均风力与阵风级
- 《降水量等级》GB/T 28592-2012：小雨 / 中雨 / 大雨 / 暴雨 / 大暴雨 / 特大暴雨（24h：≥10 / 25 / 50 / 100 / 250 mm）
- 暴雨预警信号：蓝 12h≥50、黄 6h≥50、橙 3h≥50、红 3h≥100 mm
- 《海浪预警》有效波高分级（自然资源部）：近岸 蓝2.5–3.5/黄3.5–4.5/橙4.5–6/红≥6 m；近海 黄6–9/橙9–14/红≥14 m
- 前期影响雨量 Pa 与动态临界雨量：山洪 / 地质灾害气象风险预警通行方法

---

*公益项目，非官方预警。数据仅供参考，一切以气象与应急部门发布为准。*
