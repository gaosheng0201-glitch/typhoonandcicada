# 判定口径变更日志

本文件只记录**会改变判定结果**的算法与阈值变更：改了什么、**为什么改**、依据是什么、改完实测如何。
功能性改动（界面、数据管线、抓取器）见 git 历史。

> **为什么要单独维护它**：本站的判定要能被气象、应急、水文的专业人士复核。阈值一旦变化，
> 同一场台风对同一座城市的结论就会变——必须留下可追溯的理由与实测证据，而不是只在 commit 里一笔带过。
> 完整口径见 [METHODOLOGY.md](METHODOLOGY.md)；改判定请同时更新那份与本文件。

---

## 2026-08-29 · 台风结束后自动全审计（固化为定时任务）

「每场台风结束跑一遍全审计」不再靠人记得——固化为 `storm-audit.yml`（每日北京时
凌晨 02:40）：检查停编 ≥72h（与残涡追踪上限对齐，事件完整、ERA5 大概率就绪）且未
审计的台风，`audit_storms.mjs` 自动选城（轨迹 ≤400km 最近 4 城 → 解除态全时序审计；
远洋 → 沿海 4 城零误报抽查），报告入库 **docs/data/audits/**（Pages 公开发布的审计
档案，每份附一键复现命令）。审计逻辑 spawn `sanity_check.mjs`——仍是与前端共用
assess-core 的那把唯一尺子。**发现异常 → Action 标红，即是通知。**

首轮存量建档 11 场全 PASS：巴威（宣城/威海/宿迁/芜湖——自动选城选出了与手工审计
不同但同样合理的近轨城市）、白海豚（景德镇/上饶/温州/九江）、红霞、简拉维（含台湾
桃园/新北）、20260015 热带低压，及海神/鲸鱼/灿鸿/琵鹭/浪卡远洋零误报。沙德尔/紫檀
未满 72h 待下轮自动补审；无名热带低压类 API 无详情接口者归档为跳过（不重试）。

---

## 2026-08-29 · 建站以来全部台风的存量核对（17 场清点，1 处漏报，1 处已知边界）

此前只有主要致灾场次经过系统检验，其余场次存在检验空白。本次做全量清点：探测
202606~202622 共 **17 场**，按「距中国城市最近距离」分类逐场核对。

**影响中国的 8 场**（≤400 km 有城市）：
- ✅ 已系统检验（此前）：巴威、美莎克、白海豚、沙德尔
- ✅ 本次补检通过：**红霞**（惠州/汕尾 高危期零误挂、回落准时；汕尾 275 mm/156 km/h/35h
  正确④+长雨提示）、**紫檀**（海南，此前未纳入过检验范围——海口/儋州 ③→②解除正常，
  湛江全程①）、**简拉维**（福建，同前未纳入过——泉州/莆田/厦门 ③→②解除正常）
- ⚠️ **米克拉（建站前）暴露一处真实漏报**：16 级沿台湾东侧北上，基隆实际 **163.7 mm**、
  花莲 90.3 mm，我们却判「不相关/0 mm」。根因：**象限订正在台湾失效**——城市在台风
  左侧被方向因子削减（600×≈0.4≈240 km < 343 km），但台湾东部的雨由**中央山脉地形抬升**
  主导，基于平原/海面统计（白海豚样本）的象限规律不适用。风上无碍（实际阵风仅 33~39
  km/h）。按铁律**不打单点补丁**——记为已知局限，待积累更多台湾案例后做系统性方案
  （候选方向：地形主导岸段不适用左侧削减）。

**远洋 9 场**：蔷薇/海高斯/海神/鲸鱼/灿鸿/琵鹭/浪卡/艾莎尼/艾涛——抽沿海 4 城
（上海/温州/福州/深圳）批查，**全部「不相关」零误报**。

**另记一处已知边界（紫檀审计中发现）**：事件跨度随「查看时刻」的最近点滑动，复杂
回旋路径下早期峰值可能滑出事件记忆（海口后段峰值从③回落显示为②）。行为方向安全
——解除条随之消失（宁可不说，不会说错「曾达」），非误报；留待事件跨度锚定方案。

---

## 2026-08-29 · 解除态的历史实盘审计（--audit，四场台风全通过）

不等下一场台风——用历史数据逐刻重演「当时用户看到什么」。校准脚本新增 `--audit`：
沿整场事件每 6h 一个查看时刻，每刻以「当时所知」（stormAsOf + 按时刻现算的土壤湿度、
ERA5 实况天气）重算快照与事件对象，输出「当时档 / 当时峰值 / 解除条挂不挂」全时序，
并自检翻转次数与「峰值出现前误挂」。

| 场景 | 危险期误挂 | 解除时机 | 闪烁 |
|---|---|---|---|
| 沙德尔·台州（曾4现2） | 0（高危 24h 全程不挂） | 档回落同刻出现 | 无 |
| **美莎克·北海（4→2→4 第二波）** | 0（高危 63h） | 回落时出现；**第二波高危来临时自动收回** | 出现+收回各一次，正确 |
| 美莎克·南宁 / 巴威·温州 / 白海豚·上海 | 0 | 各自峰值过后出现，此后稳定 | 无 |
| 沙德尔·丽水 / 巴威·嘉兴（全程①对照） | — | 永不出现 | 无 |

**结论**：解除条零误挂、零峰值前出现、零闪烁；停滞台风杀回马枪时「已解除」会正确
收回——绝不让这句话挂在正在变危险的屏上。ERA5 对近 5 天内的事件无数据，--audit
自动退回 forecast API 的 past_days 回溯（上限 92 天）。

---

## 2026-08-29 · 重构③：表达层消费事件对象（解除态上线）

**判定零改动**——纯表达层。panel 接线 `assessEvent`（focus 台风、5 分钟缓存）：

- **「解除态」**：事件峰值 ≥③ 且已成过去、当前档明显回落 → 明确宣布
  「『高危』已解除 —— 本次最高曾达『高危』（N 小时前），最强时段已过，当前按『准备』行动」。
  与官方「发布→解除」范式一致：不拖慢回落（对用户说谎），不抹掉峰值（事件事实留在话里）。
  沙德尔·台州「曾4现2」自此有了正式归宿——不再是用户撞见的静默降档。
- 验证：台州（椒江）正确挂条「高危已解除（35 小时前峰值）→ 当前准备」；丽水（全程①）
  无条；广州（不相关）无事件。

### 过程中发现并修复：事件断言曾建立在「演示估算」上
- **现象**：丽水全程①，却挂出「高危已解除」。
- **根因**：首次渲染时本地模式序列(loadForecast)尚未就绪，assess 走「演示估算」分支
  （按距离衰减的占位雨量）；事件采样在此分支下全程虚高到④，**且该垃圾事件对象被缓存**。
  快照渐进显示无妨（数据到位即重渲覆盖），但**事件级断言不得出自估算**。
- **修**：`getEvent` 在模式数据未就绪时返回 null 且不缓存——宁可暂缺解除条，等真实数据
  到位后由重渲补上（与 impact.json 的新鲜度保护同一哲学：过期/缺失的结论宁可不说）。
- 实测：首渲染瞬间无误报，数据就绪后台州/丽水各归其位。

---

## 2026-08-28（深夜）· 重构②：事件时间线层（判定零改动）

**收口那个唯一的架构缺口**：assess() 是「时刻快照」，而台风对一座城市是完整事件。
core 新增 `assessEvent`——用**同一个 assess** 沿事件跨度（风雨窗∪最近点，前推 12h、
后延至预报末端）每 3h 自采样，产出事件对象：`samples`（档位/阶段曲线）、`peakLevel/
peakAt`（事件峰值，不随时刻回落）、`levelPrev12h`（12 小时前会怎么说）。防御性上限
200 采样点。**level/phase 的每个值都出自同一个 assess，判定口径零改动。**

关键取景修正：采样若用「今日的末4点+今日预报」，历史强点会整个丢失（实测沙德尔
14 级掠过台州的时段在今日视角下峰值只剩②）。故新增 `stormAsOf(storm, ts)`——track
截断到「当时」、之后实况充当完美预报——每个采样点用**当时所知**取景；校准脚本的
--at 回放改调同一构造（又消一处重复）。语义如实：这是「以完整实况重演事件各时刻的
评估」，不是重放当时的机构预报（未存档）。

**验证（--event，三场台风）**：
- 沙德尔·台州 `4×30h → 2×66h`，峰值④/此刻②/12h前④ ——「曾4现2」被完整捕获，
  此前的「突兀降档」自此成为事件对象里的一条清晰事实（重构③据此渲染「已解除」）
- 美莎克·北海 `4×63h → 2×24h → 4×6h` ——高危 63 小时 + 停滞回旋的**第二波**，
  项目缘起场景的完整形状；南宁 `3×45h → 2×27h`
- 巴威·温州/台州 曾3现2；嘉兴全程①（实际仅 23.5mm）——低调正确，无误报
- 一致性自检内建：峰值 ≥ 一切采样，三场全过；不相关城市不建事件

前端本次仅共享 core 文件（无 UI 消费）——表达层接线在重构③。

---

## 2026-08-28（晚）· 重构①：评估核心抽出为单一尺子（判定零改动）

**动机**：校准脚本(sanity_check.py)是 assess() 的 Python 平行复刻——验收工具与被验收
对象是两套实现，验收本身不可信。本次把评估核心抽为 `docs/assess-core.js` 纯函数
（无 DOM、无 fetch、无 Date.now()——「现在」永远是 nowT 参数），前端(panel.js)与
新校准脚本(scripts/sanity_check.mjs, node)**共用同一文件**；estGaleRadius 权威表仍在
data.js，由 configure() 注入不复制。Python 版退役。

**判定口径零改动**——纯迁移。但迁移对齐过程**清点出 Python 复刻的 7 处漂移**（即过去
拿它验收时的系统性偏差）：easing 未实现、postRain24 回落未实现、histClosest 的
「须已成过去」时间条件未实现、Pa 公式最近一天权重 K⁰ vs 前端 K¹、path 用全轨迹 vs
前端「末4+预报」、nowWx 缺失、level 回落规则缺失。**自此校准脚本与前端不可能再漂**。

**迁移验收**：三场台风（美莎克/巴威/沙德尔）回放+当前+sweep，node 版与 Python 版
逐字段对比，除上述 7 处漂移造成的差异外完全一致；浏览器端到端（温州·沙德尔）
档位/最强时刻/进度/时间线/对照/清单全部正常。回放构造同时修正：--at 时 track 截断到
「当时」、之后实况充当完美预报（此前全轨迹直入会破坏 fwdClosest/strongSoon 语义）。

### 修订 08-28 早间「未解决：时序跳变」条目
同核复跑后澄清：台州「12 小时档4→档2」**不是算法缺陷**——01:59 时当时预报确有
14 级将贴身而过（档4 正确），14:36 预报兑现、台风减弱远离（档2 正确），这是
**预报兑现后的正常回落**，且「本次最强时刻」一行已把 14 级掠过的事实保留在页面上。
原条目的「跳变证据」来自 Python 第 5 处漂移（path 用全轨迹，把已成过去的 14 级点
误算进 strongSoon 的「未来」），显示方式放大了突兀感。**该项关闭**；后续若做平滑，
按三条铁律走系统性流程。

---

## 2026-08-28 · 沙德尔实盘核查（含一个**未解决**问题）

### 已改：安全网只看「此刻或未来」的中心强度，不看历史
- **问题**：安全网用 `closest` 点的强度，而 closest 可能早已过去。沙德尔实测——14 级中心从台州
  52 km 外掠过 **7 小时后**，中心已远到 199 km、减弱为 10 级、台州实际只下 31 mm/阵风 10 级，
  却仍判「高危·听从官方转移安排」。
- **现**：改用插值到此刻的中心（`dNow < 100 && cNow.power ≥ 14`）或未来会压境（`strongSoon`）。

### 已加：界面补「本次最强时刻」（事件级事实）
- 行动等级会随台风远离而回落，但「曾以 14 级从你 52 km 外掠过」是这个事件最危险的部分，
  **不该被一起抹掉**。故新增一行：「本次最强时刻：N 小时前以 X 级从你 Y km 外经过，本地峰值阵风约 Z 级」。

### ⚠️ 未解决：档位随阶段的**时序跳变**
- `--sweep` 实测：台州/温州在 08-26 ~ 08-28 01:59 一路档4（未来将有 14 级压境 → 合理），
  到 08-28 13:59 突降档2（台风已过、实际仅 31 mm → 也合理），**但 12 小时内从「高危·听从转移」
  变成「准备·物资检查」，落差过大**。
- **这是「针对单一时刻打补丁」的直接后果**——修正流程见 [CONTRIBUTING「改判定逻辑的流程」](CONTRIBUTING.md#改判定逻辑的流程阈值算法类改动必读)。
- **处置**：现有阈值架构（风雨+土壤湿度→影响等级）本身没问题，**不推翻**；界面已补「本次最强
  时刻」保住事件事实。跳变本身留待下次**系统性排查**（多城市×多时刻整体验证）时一并处理，
  不做单点补丁。历史对照的定位维持原设计：**城市排水能力的代理参考**（该能力无直接数据、且随
  城市改造变化，历史仅供体感参考）——是辅助，不是主判据。

---

## 2026-08-11（下午）· 「两套尺子」专项审计

主动排查「同一个量在两处用不同口径」——此前已因此撞了三次（level vs tier 的雨臂、
level vs tier 的风臂、风圈 vs 雨半径）。本次全量扫描代码与文档，结果：

### 已修：AI 多期会商的影响半径改为动态（原固定 150 km）
- **问题**：`build_impact.py` 用**固定 150 km + 时效误差**判「台风是否波及该市」，主判定却用
  **随强度动态的风圈（70~400 km）**。同一个「会不会波及这座城市」，两处两把尺子。
- **现**：改为按该预报点的强度取半径，**并且直接从 `docs/data.js` 解析 `estGaleRadius` 那张表**
  （而不是在 Python 里复制一份）——本项目已因「同一个量两处算」踩坑四次，故这次做成真正的
  单一数据源：前端表一改，后端自动跟随；表结构变了脚本会直接报错而不是悄悄用旧值。
  FNV3 的风速单位是节，按蒲福风级换算成风力等级后查表；该点无强度时兜底 150 km。
- **实测**：白海豚命中城市 71 → 58（弱台风阶段半径收窄，更精准）；high 档为信阳/孝感/武汉
  ——正是台风中心实际经过的地方。其余远洋台风命中 0 城，无误报。

> **一处需要更正的记录**：本条目最初写作「长三角主判定说影响进行中、AI 会商却说未逼近，
> 用户会看到自相矛盾」。复核发现**那是审计脚本自己查错了台风**（取了 `storms[0]`，即远洋的
> 灿鸿，而非白海豚）——白海豚在旧口径下长三角本来就是命中的，**该矛盾并不存在**。
> 半径口径不一致是真的，故对齐仍然有价值；但结论不该建立在错误的核对上，记此更正。

### 已改：AI 会商措辞明确「中心 ≠ 影响」
- AI 会商回答的是「**台风中心**会不会经过你」，主判定回答「你**会不会受影响**」，两者本就不同。
  台州即是实例：白海豚期间中心「未逼近」，本地却下了 **527 mm**。
- 故措辞改为「**台风中心**落点都不在 XX 附近——**中心不经过 ≠ 不受影响**，外围风雨以上方判断为准」，
  避免被读成「不影响你」。

### 已修：残涡追踪时长的注释漂移
- `data.js` 注释写「停编 ≤48h」，**实际是 `RESIDUAL_MAX_H = 72`**，且还要求
  残涡区仍有 ≥30 mm/24h 降雨预报。注释已按实际行为更正。

### 复查通过（确认无第二把尺子）
- **影响半径**：`estGaleRadius` 是全站唯一权威表，`panel.js`（评估/分享卡）与 `app.js`（地图风圈）
  同引一处 ✓
- **归因门槛**：1.5 mm/h、62 km/h、6 h 分段——`panel.js` 与 `sanity_check.py` 一致 ✓
- **海浪分级**：2.5/4/6/9 m，代码与 METHODOLOGY 一致 ✓
- **残余雨门槛**：`postRain24 ≥ 30` 四处取值一致 ✓
- **分享卡 / 应急手册长图 / 底部摘要条**：均复用 `assessAll()` 结果，无重算 ✓
- **`build_history` / `build_rain_history` 的 100 / 300 km**：是**历史统计口径**（历史上多少次台风
  靠近过你），与前端「这一次的影响半径」用途不同，不构成冲突 ✓
- **`build_consensus`**：用自校准的 r68/r90 不确定性锥，不涉及影响判定 ✓

---

## 2026-08-11

### 风险档·风臂：改用本地实际预报的峰值阵风
- **原**：`中心距离 <200km 且 中心强度 ≥10 级` 的几何代理。
- **问题**：系统性**过度预警**。巴威回测（2026-07-12，浙江 8 市）——巴威是 10~13 级、各市都在
  200 km 内，于是 **8 城全判「戒备」**；可实测峰值阵风只有 8~9 级，嘉兴过程雨量仅 23.5 mm。
  **中心多强 ≠ 你这里多大风。**
- **现**：阵风 ≥10 级→②、≥12 级→③、≥14 级→④（对齐《台风预警信号》）。无模式数据时才回退
  几何代理；保留安全网：强台风中心 <100 km 正面压境直接④，防模式低估极端阵风。
- **实测**：温州（10 级）、台州（12 级）仍③；丽水/金华→②；杭州/宁波/绍兴/嘉兴→①。
  另一端不漏：白海豚·台州 527 mm/12 级→④，美莎克·北海 261 mm/持续 60 h→④。

### 「停留型」拆成两条轴，对用户只呈现一条
- **原**：一个「停留型台风 —— 移速仅约 X km/h」标签。
- **问题**：白海豚移速 17.5~36 km/h **一点不慢**，叫停留型名不副实；而**移速是路径图的技术参数，
  用户要的是「这个阶段对我什么影响」**。
- **现**：内部分 `slowThreat`（台风赖着不走，移速<10 km/h）与 `longRain`（本地风雨窗口≥24 h）；
  对用户合并成一句：「**风雨持续时间长 —— 预计持续两天以上，雨量会不断累积，重点防内涝而不是防风**」，
  移速降级为灰色括号补充。

### 「停留型」补上「后果」这条腿
- **原**：移速慢 **或** 滞留久，即判停留型。
- **问题**：滞留 30 h 却只下 20 mm 也会被标；而移速不慢却下 300 mm 反倒不标。
- **现**：三条腿缺一不可——①中心进影响半径 ②移速<10 km/h 或滞留≥24 h ③过程雨量 ≥50 mm×(1−0.4w)。
  停留型真正致灾的是「雨长时间累积在同一处」（美莎克广西 600 mm+），只讲滞留不讲累积，
  等于把「慢」本身当危险。

### 风险档·雨臂：随土壤湿度动态下调（「高危」线除外）
- **问题**：`level` 用固定 60/150/250、`localImpactTier` 却用动态阈值——两套尺子。杭州连下三天
  土壤饱和(w=0.81)，只因未来预报下调让总量 162→139 跨回 150 以下就降档，可台风还在影响中。
- **现**：雨臂统一 `×(1−0.4w)`。但**「高危」的雨量线不下调**——其文案涉及听从转移安排，
  土壤饱和时 187 mm 就把整片长三角推到转移级别，过度了。

### 事件完整性：过境后不得退化成「风雨会来」
- **问题**：模式序列只取 `past_days=2`，台风过境两天后那段雨滑出序列 → 窗口消失 → phase 退回
  approach。实测红霞（最近点距广州 91 km/9 级、**15 天前**）会显示「风雨会来，备点吃喝更安心」。
- **现**：`past_days` 2→7 盖住整个事件周期；并加逻辑兜底——拿不到窗口但最近点确已成过去 ⇒ `after`。

### 雨量表达：不做「总量 − 已下 = 还剩」的减法
- **问题**：模式雨量误差本就大、机构还会不断调整，减出来的「还剩 20 mm」是假精度。
- **现**：给**进度感**——刚开始 / 正处在影响较强时段 / 已过大半接近尾声 / 已基本结束。

### 内涝清单不再等用户自认「住低洼」
- **问题**：防内涝的精准条目只挂在「低洼」「商铺」情境下，要用户主动勾选才出现。上海白海豚实地
  调研：别墅负一楼与临街商铺进水，而这些人未必会这么给自己归类。
- **现**：雨量达内涝量级（50 mm×(1−0.4w)）且尚未过境时，防内涝动作推给所有人。
  **实测**：上海 08-09 07:03、才下 24 mm 时就已推送，早于进水一天多。

---

## 2026-08-10

### 降雨相关半径：风的范围 ≠ 雨的范围
- **问题**：拿 7 级风圈（衡量风）去判「这场雨算不算台风的账」，量纲用错。白海豚中心距杭州 257 km
  超出「风圈 160×1.25=200」被判「与台风无关」，可杭州正连下三天、累计 186 mm 大暴雨；
  宁波(199)、上海(193)、南京(102) 同样被漏掉。
- **现**：雨单独用 `风圈×2.5` 夹 [300,600] km。风的判定（档位风臂、inRange、停留型）仍用风圈。

### 降水不对称：象限订正
- **依据**：白海豚实测——移动方向**右前象限** 5 城平均 147 mm，左侧 3 城仅 11 mm（**13 倍**），
  距离完全解释不了（福州 159 km 只 11 mm，上海 348 km 却 193 mm）。文献亦证强降水位于路径前进
  方向右侧。
- **现**：相关半径 × `0.65+0.35·cos(相对方位−45°)`，下限 150 km。福州（左后）、武汉（左前）
  因此被正确排除。

### 已发生 / 未来分离
- **问题**：窗口随时间滑动，同一场台风昨天 186 mm、今天 0 mm，结论自相矛盾。
- **现**：拆成「已下（既成事实，不改写）」与「还会下（预警主角，持续更新）」。
  稳定性实测（杭州/上海/宁波跨 3 天 7 时刻）：过程总量恒定、已下单调增、档位不跳变、相关性从不翻转。

### 本地气候标定（历史分位数）
- **依据**：同样下 100 mm，在屏东排第 **16** 百分位（中位 216 mm），在北京却是**有记录以来最大**
  （中位仅 1.9 mm）——全国一把尺不科学。
- **现**：在历史对照中给出「本次 N mm 排本地第 P 百分位（近 M 场样本）」。数据来自
  `rain-percentile.json`（262 城×85 台风 ERA5 聚合，177 城收录、140 城样本≥10）。
  **仅作表达与标定，不直接改判档**——分位数是「罕见度」，不等于「后果严重度」。

---

## 2026-07-25

### 停留型：移速门槛 18 → 10 km/h，并加入滞留时长
- **依据**：西北太平洋台风常态移速 15~20 km/h（快的 30），真正「移动缓慢」是 5~10（海葵登陆后
  5–10、烟花平均<10）。原 18 km/h 正落在常态区间中段，**几乎所有台风都会被误判为停留型**。
- **现**：<10 km/h；另加「本地风雨滞留 ≥24 h」作为更本质的判据——移速不慢却贴着你走的台风
  原先会漏判。

---

## 2026-07-13

### 阶段判定：改用台风中心的实际位置，而不是本地雨窗
- **问题**：弱台风贴着你走时雨会暂歇，雨窗一关就翻「已过境」。青岛市南实测：中心还在隔壁 37 km、
  还在下雨，却报「已过境，本地以外围影响为主」；相邻的莱西反而说「减弱中」。
- **现**：把轨迹+预报插值到此刻，只有「中心确已离开影响半径且在远离」+ 本地雨窗也结束，才判 after。

### 停留型：加距离闸门
- **问题**：刚生成、远在洋面上的弱台风（海神）因移速慢被贴「停留型」。
- **现**：中心必须确实进入你的影响半径。

---

*所有阈值与其官方标准对照见 [METHODOLOGY.md](METHODOLOGY.md)。回归自检：
`python3 scripts/sanity_check.py <tfid> [--at 2026-07-04T12] [--cities 南宁市,北海市] [--sweep]`*
