# 判定口径变更日志

本文件只记录**会改变判定结果**的算法与阈值变更：改了什么、**为什么改**、依据是什么、改完实测如何。
功能性改动（界面、数据管线、抓取器）见 git 历史。

> **为什么要单独维护它**：本站的判定要能被气象、应急、水文的专业人士复核。阈值一旦变化，
> 同一场台风对同一座城市的结论就会变——必须留下可追溯的理由与实测证据，而不是只在 commit 里一笔带过。
> 完整口径见 [METHODOLOGY.md](METHODOLOGY.md)；改判定请同时更新那份与本文件。

---

## 2026-08-11（下午）· 「两套尺子」专项审计

主动排查「同一个量在两处用不同口径」——此前已因此撞了三次（level vs tier 的雨臂、
level vs tier 的风臂、风圈 vs 雨半径）。本次全量扫描代码与文档，结果：

### 已修：AI 多期会商的影响半径改为动态（原固定 150 km）
- **问题**：`build_impact.py` 用**固定 150 km + 时效误差**判「台风是否波及该市」，主判定却用
  **随强度动态的风圈（70~400 km）**。同一个「会不会波及这座城市」，两处两把尺子。
- **现**：改为按该预报点的强度取半径，**并且直接从 `docs/data.js` 解析 `estGaleRadius` 那张表**
  （而不是在 Python 里复制一份）——本项目已因「同一个量两处算」踩坑四次，故这次做成真正的
  单一数据源：前端表一改，后端自动跟随；表结构变了脚本会直接报错而不是悄悄用旧值。
  FNV3 的风速单位是节，按蒲福风级换算成风力等级后查表；该点无强度时兜底 150 km。
- **实测**：白海豚命中城市 71 → 58（弱台风阶段半径收窄，更精准）；high 档为信阳/孝感/武汉
  ——正是台风中心实际经过的地方。其余远洋台风命中 0 城，无误报。

> **一处需要更正的记录**：本条目最初写作「长三角主判定说影响进行中、AI 会商却说未逼近，
> 用户会看到自相矛盾」。复核发现**那是审计脚本自己查错了台风**（取了 `storms[0]`，即远洋的
> 灿鸿，而非白海豚）——白海豚在旧口径下长三角本来就是命中的，**该矛盾并不存在**。
> 半径口径不一致是真的，故对齐仍然有价值；但结论不该建立在错误的核对上，记此更正。

### 已改：AI 会商措辞明确「中心 ≠ 影响」
- AI 会商回答的是「**台风中心**会不会经过你」，主判定回答「你**会不会受影响**」，两者本就不同。
  台州即是实例：白海豚期间中心「未逼近」，本地却下了 **527 mm**。
- 故措辞改为「**台风中心**落点都不在 XX 附近——**中心不经过 ≠ 不受影响**，外围风雨以上方判断为准」，
  避免被读成「不影响你」。

### 已修：残涡追踪时长的注释漂移
- `data.js` 注释与维护手册两处都写「停编 ≤48h」，**实际是 `RESIDUAL_MAX_H = 72`**，且还要求
  残涡区仍有 ≥30 mm/24h 降雨预报。注释已按实际行为更正。

### 复查通过（确认无第二把尺子）
- **影响半径**：`estGaleRadius` 是全站唯一权威表，`panel.js`（评估/分享卡）与 `app.js`（地图风圈）
  同引一处 ✓
- **归因门槛**：1.5 mm/h、62 km/h、6 h 分段——`panel.js` 与 `sanity_check.py` 一致 ✓
- **海浪分级**：2.5/4/6/9 m，代码与 METHODOLOGY 一致 ✓
- **残余雨门槛**：`postRain24 ≥ 30` 四处取值一致 ✓
- **分享卡 / 应急手册长图 / 底部摘要条**：均复用 `assessAll()` 结果，无重算 ✓
- **`build_history` / `build_rain_history` 的 100 / 300 km**：是**历史统计口径**（历史上多少次台风
  靠近过你），与前端「这一次的影响半径」用途不同，不构成冲突 ✓
- **`build_consensus`**：用自校准的 r68/r90 不确定性锥，不涉及影响判定 ✓

---

## 2026-08-11

### 风险档·风臂：改用本地实际预报的峰值阵风
- **原**：`中心距离 <200km 且 中心强度 ≥10 级` 的几何代理。
- **问题**：系统性**过度预警**。巴威回测（2026-07-12，浙江 8 市）——巴威是 10~13 级、各市都在
  200 km 内，于是 **8 城全判「戒备」**；可实测峰值阵风只有 8~9 级，嘉兴过程雨量仅 23.5 mm。
  **中心多强 ≠ 你这里多大风。**
- **现**：阵风 ≥10 级→②、≥12 级→③、≥14 级→④（对齐《台风预警信号》）。无模式数据时才回退
  几何代理；保留安全网：强台风中心 <100 km 正面压境直接④，防模式低估极端阵风。
- **实测**：温州（10 级）、台州（12 级）仍③；丽水/金华→②；杭州/宁波/绍兴/嘉兴→①。
  另一端不漏：白海豚·台州 527 mm/12 级→④，美莎克·北海 261 mm/持续 60 h→④。

### 「停留型」拆成两条轴，对用户只呈现一条
- **原**：一个「停留型台风 —— 移速仅约 X km/h」标签。
- **问题**：白海豚移速 17.5~36 km/h **一点不慢**，叫停留型名不副实；而**移速是路径图的技术参数，
  用户要的是「这个阶段对我什么影响」**。
- **现**：内部分 `slowThreat`（台风赖着不走，移速<10 km/h）与 `longRain`（本地风雨窗口≥24 h）；
  对用户合并成一句：「**风雨持续时间长 —— 预计持续两天以上，雨量会不断累积，重点防内涝而不是防风**」，
  移速降级为灰色括号补充。

### 「停留型」补上「后果」这条腿
- **原**：移速慢 **或** 滞留久，即判停留型。
- **问题**：滞留 30 h 却只下 20 mm 也会被标；而移速不慢却下 300 mm 反倒不标。
- **现**：三条腿缺一不可——①中心进影响半径 ②移速<10 km/h 或滞留≥24 h ③过程雨量 ≥50 mm×(1−0.4w)。
  停留型真正致灾的是「雨长时间累积在同一处」（美莎克广西 600 mm+），只讲滞留不讲累积，
  等于把「慢」本身当危险。

### 风险档·雨臂：随土壤湿度动态下调（「高危」线除外）
- **问题**：`level` 用固定 60/150/250、`localImpactTier` 却用动态阈值——两套尺子。杭州连下三天
  土壤饱和(w=0.81)，只因未来预报下调让总量 162→139 跨回 150 以下就降档，可台风还在影响中。
- **现**：雨臂统一 `×(1−0.4w)`。但**「高危」的雨量线不下调**——其文案涉及听从转移安排，
  土壤饱和时 187 mm 就把整片长三角推到转移级别，过度了。

### 事件完整性：过境后不得退化成「风雨会来」
- **问题**：模式序列只取 `past_days=2`，台风过境两天后那段雨滑出序列 → 窗口消失 → phase 退回
  approach。实测红霞（最近点距广州 91 km/9 级、**15 天前**）会显示「风雨会来，备点吃喝更安心」。
- **现**：`past_days` 2→7 盖住整个事件周期；并加逻辑兜底——拿不到窗口但最近点确已成过去 ⇒ `after`。

### 雨量表达：不做「总量 − 已下 = 还剩」的减法
- **问题**：模式雨量误差本就大、机构还会不断调整，减出来的「还剩 20 mm」是假精度。
- **现**：给**进度感**——刚开始 / 正处在影响较强时段 / 已过大半接近尾声 / 已基本结束。

### 内涝清单不再等用户自认「住低洼」
- **问题**：防内涝的精准条目只挂在「低洼」「商铺」情境下，要用户主动勾选才出现。上海白海豚实地
  调研：别墅负一楼与临街商铺进水，而这些人未必会这么给自己归类。
- **现**：雨量达内涝量级（50 mm×(1−0.4w)）且尚未过境时，防内涝动作推给所有人。
  **实测**：上海 08-09 07:03、才下 24 mm 时就已推送，早于进水一天多。

---

## 2026-08-10

### 降雨相关半径：风的范围 ≠ 雨的范围
- **问题**：拿 7 级风圈（衡量风）去判「这场雨算不算台风的账」，量纲用错。白海豚中心距杭州 257 km
  超出「风圈 160×1.25=200」被判「与台风无关」，可杭州正连下三天、累计 186 mm 大暴雨；
  宁波(199)、上海(193)、南京(102) 同样被漏掉。
- **现**：雨单独用 `风圈×2.5` 夹 [300,600] km。风的判定（档位风臂、inRange、停留型）仍用风圈。

### 降水不对称：象限订正
- **依据**：白海豚实测——移动方向**右前象限** 5 城平均 147 mm，左侧 3 城仅 11 mm（**13 倍**），
  距离完全解释不了（福州 159 km 只 11 mm，上海 348 km 却 193 mm）。文献亦证强降水位于路径前进
  方向右侧。
- **现**：相关半径 × `0.65+0.35·cos(相对方位−45°)`，下限 150 km。福州（左后）、武汉（左前）
  因此被正确排除。

### 已发生 / 未来分离
- **问题**：窗口随时间滑动，同一场台风昨天 186 mm、今天 0 mm，结论自相矛盾。
- **现**：拆成「已下（既成事实，不改写）」与「还会下（预警主角，持续更新）」。
  稳定性实测（杭州/上海/宁波跨 3 天 7 时刻）：过程总量恒定、已下单调增、档位不跳变、相关性从不翻转。

### 本地气候标定（历史分位数）
- **依据**：同样下 100 mm，在屏东排第 **16** 百分位（中位 216 mm），在北京却是**有记录以来最大**
  （中位仅 1.9 mm）——全国一把尺不科学。
- **现**：在历史对照中给出「本次 N mm 排本地第 P 百分位（近 M 场样本）」。数据来自
  `rain-percentile.json`（262 城×85 台风 ERA5 聚合，177 城收录、140 城样本≥10）。
  **仅作表达与标定，不直接改判档**——分位数是「罕见度」，不等于「后果严重度」。

---

## 2026-07-25

### 停留型：移速门槛 18 → 10 km/h，并加入滞留时长
- **依据**：西北太平洋台风常态移速 15~20 km/h（快的 30），真正「移动缓慢」是 5~10（海葵登陆后
  5–10、烟花平均<10）。原 18 km/h 正落在常态区间中段，**几乎所有台风都会被误判为停留型**。
- **现**：<10 km/h；另加「本地风雨滞留 ≥24 h」作为更本质的判据——移速不慢却贴着你走的台风
  原先会漏判。

---

## 2026-07-13

### 阶段判定：改用台风中心的实际位置，而不是本地雨窗
- **问题**：弱台风贴着你走时雨会暂歇，雨窗一关就翻「已过境」。青岛市南实测：中心还在隔壁 37 km、
  还在下雨，却报「已过境，本地以外围影响为主」；相邻的莱西反而说「减弱中」。
- **现**：把轨迹+预报插值到此刻，只有「中心确已离开影响半径且在远离」+ 本地雨窗也结束，才判 after。

### 停留型：加距离闸门
- **问题**：刚生成、远在洋面上的弱台风（海神）因移速慢被贴「停留型」。
- **现**：中心必须确实进入你的影响半径。

---

*所有阈值与其官方标准对照见 [METHODOLOGY.md](METHODOLOGY.md)。回归自检：
`python3 scripts/sanity_check.py <tfid> [--at 2026-07-04T12] [--cities 南宁市,北海市] [--sweep]`*
